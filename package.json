{
  "name": "tutor-verification-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^0.18.0",
    "json-server": "^0.14.0",
    "react": "^16.6.3",
    "react-dom": "^16.6.3",
    "react-router-dom": "^4.3.1",
    "react-scripts": "2.1.1",
    "tuteria-shared": "gbozee/Shared-Admin-Lib#28d6bf0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "copy_now": "cp now.json.sample ./build/now.json",
    "deploy": "now --no-clipboard --token=$NOW_TOKEN --build-env REACT_APP_FIREBASE_API_KEY=$FIREBASE_API_KEY  --build-env REACT_APP_FIREBASE_PROJECT_ID=$FIREBASE_PROJECT_ID --build-env REACT_APP_ENDPOINT_URL=$ENDPOINT_URL && now alias"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [">0.2%", "not dead", "not ie <= 11", "not op_mini all"],
  "devDependencies": {}
}
